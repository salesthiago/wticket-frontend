// Este arquivo pode ser substituído durante o build usando a opção `fileReplacements`.
// `ng build` substitui `environment.ts` com `environment.prod.ts`.
// A lista de substituições de arquivo pode ser encontrada em `angular.json`.

export const environment = {
    production: false,
    apiUrl: 'http://localhost:3000/api',
    socketUrl: 'http://localhost:3000',
    enableDebug: true
};
